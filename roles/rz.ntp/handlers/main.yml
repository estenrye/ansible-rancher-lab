---
 - name: restart ntp
   service:
     name: "{{ ntp_client }}"
     state: restarted
   when: ntp_enabled

 - name: restart ntpd
   service:
     name: "{{ ntp_daemon }}"
     state: restarted
   when: ntpd_enabled