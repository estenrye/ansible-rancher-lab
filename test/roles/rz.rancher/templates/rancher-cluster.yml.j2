---
nodes:
{% for item in rke_nodes %}
  - address: {{item.address}}
    user: {{username}}
	role:
	{% for role in item.roles %} 
	  - {{role}}
	{% endfor %}
	ssh_key_path: {{ssh_key_path}}
{% endfor %}
